apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080  # VM 공인IP:30080 으로 브라우저에서 직접 접속 가능하도록 설정
  type: NodePort